#include <stdio.h>


int main(void) {
    /* NOTE(stitaevskiy):
     * You could check your implementation here.
     */
    /* 
    Matrix* matrix = create_matrix(2,2);
    set_elem(matrix, 0, 0, 0);
    set_elem(matrix, 0, 1, 1);
    set_elem(matrix, 1, 0, 2);
    set_elem(matrix, 1, 1, 3);
    Matrix* matrix1 = create_matrix(2,2);
    set_elem(matrix1, 0, 0, 0);
    set_elem(matrix1, 0, 1, 1);
    set_elem(matrix1, 1, 0, 2);
    set_elem(matrix1, 1, 1, 3);
    Matrix* ans = sum(matrix, matrix1);
    double v0;
    double v1;
    double v2;
    double v3;
    get_elem(ans, 0, 0, &v0);
    get_elem(ans, 0, 1, &v1);
    get_elem(ans, 1, 0, &v2);
    get_elem(ans, 1, 1, &v3);
    if (v0 != 0 && v1 != 2 && v2 != 4 && v3 != 6){
        return 1;
    }
    */
    return 0;
}

